{% extends 'base.html.twig' %}

{% block title %}- Annonces{% endblock %}

{% block body %}

    <h1> Listes des annonces</h1>
    <div class="adverts_list">
        <div class="container">
            <div class="row">
                <p>Liste des annonces texte</p>
                <div class="col-5">
                    <div>
                        <div class="col-4">Nom de l'annonce</div>
                        <div class="col-4">Date de création</div>
                        <div class="col-4">auteur de l'annonce</div>
                    </div>

                    {% for advertText in advertsText %}
                        <div>
                            <div class="col-4">{{ advertText.advertName }}</div>
                            <div class="col-4">{{ advertText.createdAt | date("d/m/Y") }}</div>
                            <div class="col-4">nom du créateur</div>
                        </div>
                    {% endfor %}
                </div>

                <p>Liste des annonces image</p>
                <div class="col-5">
                    <div>
                        <div class="col-4">Nom de l'annonce</div>
                        <div class="col-4">Date de création</div>
                        <div class="col-4">auteur de l'annonce</div>
                    </div>
                    {% for advertImage in advertsImage %}
                        <div>
                            <div class="col-4">{{ advertImage.advertName }}</div>
                            <div class="col-4">{{ advertImage.createdAt | date("d/m/Y") }}</div>
                            <div class="col-4">nom du créateur</div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
